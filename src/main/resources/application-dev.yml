spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seckill_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
  
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.secskill: DEBUG

